<!DOCTYPE html>
<html>
<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <title>Booking Details</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-image: url("public/images/bg2_payment.jpeg");
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .booking-details {
            border-radius: 5px;
            margin: 20px auto;
            padding: 70px; /* Increased padding for a larger white layer */
            max-width: 1000px; /* Increased max-width for more space */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            background-color: rgba(255, 255, 255, 0.8);
        }

        h1 {
            color: #333;
        }

        .booking-info {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
        }

        .info-left, .info-right {
            width: 45%; /* Reduced width to add more space between columns */
            margin: 0 55px; /* Added margin to create a gap between columns */
            text-align: left; /* Align text to the left for the left column */
        }

        .info-right {
            text-align: right; /* Align text to the right for the right column */
        }

        .info-left p, .info-right p {
            margin: 22px 0; /* Increased margin for more spacing between data */
            color: #333;
        }

        .info-left strong, .info-right strong {
            font-weight: bold;
        }

        .info-right p {
            white-space: nowrap;
        }

        .booking-status p {
            background-color: #4CAF50;
            color: #fff;
            padding: 10px;
            border-radius: 5px;
            font-weight: bold;
            margin: 20px 0; /* Increased margin for more spacing */
            
        }

        .cancel-button {
            font-size: 18px;
                font-weight: bold;
                text-decoration: none;
                padding: 15px 30px;
                border-radius: 8px;
                background-color: rgb(198, 76, 76);
                color: black;
                border: 2px solid rgb(198, 76, 76);
                transition: all 0.3s ease;
                cursor: pointer;
                margin-right: 20px;
                transition: transform 0.3s ease-in-out;
                

        }

        .cancel-button:hover {
            background-color: #cc0000;
            transform: scale(1.2);

        }
        .navbar {
        position: fixed;
        top: 0;
        right: 0;
         /* Adjust the background color as needed */
        padding: 10px;
        z-index: 1000; /* Ensure the navbar is above other elements */
    }

    .navbar a {
        color: white;
        font-family: fantasy ; /* Adjust the text color as needed */
        text-decoration: none;
        font-size: 18px;
        margin-right: 5px; /* Adjust the margin to shorten the gap */
    }
    .logo {
        width: 18px; /* Set the width to match the "Home" link */
        height: 18px; /* Set the height to match the "Home" link */
        margin-right: 10px;
        color: white; /* Adjust the margin as needed */
    }
    </style>
</head>
<body>
    <div class="navbar">
        <a href="/">Home</a>
        <img src="/public/images/menu.png" alt="Menu Logo" class="logo">
    </div>
    <div class="booking-details">
        <h2>Save the information in the following table for further inquiry</h2>
        <h1 style="color: rgb(188, 118, 118);">Booking Details</h1>
        <div class="booking-info">
            <div class="info-left">
                <h2>
                    <b>
                        <p><strong style="color: rgb(188, 118, 118);">Reservation ID :</strong> <%= bookingData.reservationId %></p>
                        <p><strong style="color: rgb(188, 118, 118);">First Name :</strong> <%= bookingData.firstName %></p>
                        <p><strong style="color: rgb(188, 118, 118);">Last Name :</strong> <%= bookingData.lastName %></p>
                        <p><strong style="color: rgb(188, 118, 118);">Email :</strong> <%= bookingData.email %></p>
                    </b>
                </h2>
            </div>
            <div class="info-right">
                <h2>
                    <p><strong style="color:rgb(188, 118, 118);">Check-In Date :</strong> <%= bookingData.checkInDate %></p>
                    <p><strong style="color: rgb(188, 118, 118);">Check-Out Date :</strong> <%= bookingData.checkOutDate %></p>
                    <p><strong style="color: rgb(188, 118, 118);">Reservation status :</strong> <%= bookingData.reservationStatus %></p>
                </h2>
            </div>
        </div>
        <div class="booking-status">
            <a href="/cancel_book">
                <button class="cancel-button">Cancel Booking</button>
            </a>
           
        </div>
    </div>
</body>
</html>